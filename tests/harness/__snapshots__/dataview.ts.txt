dataview.ts:9:7 - error SC2020: 'ArrayBuffer' is part of the standard library types but has no scriptc lowering yet

   8 | // '.buffer' only compiles inside new DataView(x.buffer, ...).
   9 | const escaped = buf.buffer;
     |       ^~~~~~~
  10 | 

  hint: no free-standing ArrayBuffer value exists — typed arrays own their storage (new Uint8Array(n) allocates; new Uint8Array(new ArrayBuffer(n)) erases the buffer into the view)

dataview.ts:9:17 - error SC1090: '.buffer' outside new DataView(x.buffer, ...) / Buffer.from(x.buffer, ...) (no free-standing ArrayBuffer value exists; subarray() answers an aliasing view directly) is not supported yet

   8 | // '.buffer' only compiles inside new DataView(x.buffer, ...).
   9 | const escaped = buf.buffer;
     |                 ^~~~~~~~~~
  10 | 

dataview.ts:12:7 - error SC2020: 'ArrayBuffer' is part of the standard library types but has no scriptc lowering yet

  11 | // The constructor wants a typed array's own '.buffer'.
  12 | const ab = new ArrayBuffer(8);
     |       ^~
  13 | const direct = new DataView(ab);

  hint: no free-standing ArrayBuffer value exists — typed arrays own their storage (new Uint8Array(n) allocates; new Uint8Array(new ArrayBuffer(n)) erases the buffer into the view)

dataview.ts:12:12 - error SC2020: 'new ArrayBuffer' is part of the standard library types but has no scriptc lowering yet

  11 | // The constructor wants a typed array's own '.buffer'.
  12 | const ab = new ArrayBuffer(8);
     |            ^~~~~~~~~~~~~~~~~~
  13 | const direct = new DataView(ab);

  hint: no free-standing ArrayBuffer value exists — typed arrays own their storage: allocate the view directly (new Uint8Array(n)), or erase a fresh buffer into one (new Uint8Array(new ArrayBuffer(n)), new DataView(new ArrayBuffer(n), ...))

dataview.ts:13:29 - error SC2020: 'new DataView over this buffer expression' is part of the standard library types but has no scriptc lowering yet

  12 | const ab = new ArrayBuffer(8);
  13 | const direct = new DataView(ab);
     |                             ^~
  14 | void view;

  hint: views compile over a typed array's own storage — new DataView(x.buffer, byteOffset?, byteLength?) where x is a Uint8Array/Uint32Array/Float32Array/Float64Array/Buffer value — or a fresh buffer erased into the view: new DataView(new ArrayBuffer(n), ...); free-standing ArrayBuffers have no representation