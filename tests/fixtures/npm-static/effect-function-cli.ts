// Real published Effect JavaScript, compiled as ordinary static modules.
// Module initialization uses parameterized functions that read `arguments`.
import { identity } from "effect/Function";

console.log(identity("Effect static"));
